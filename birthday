import React from 'react';
import { motion } from 'framer-motion';
import { FiArrowRight, FiActivity, FiLayers, FiCpu } from 'react-icons/fi';

export default function AnimatedWebsite() {
  // Animation variants for staggered orchestrations
  const containerVariants = {
    hidden: { opacity: 0 },
    visible: {
      opacity: 1,
      transition: { staggerChildren: 0.2 }
    }
  };

  const itemVariants = {
    hidden: { y: 30, opacity: 0 },
    visible: { 
      y: 0, 
      opacity: 1, 
      transition: { duration: 0.6, ease: "easeOut" } 
    }
  };

  return (
    <div className="min-h-screen bg-slate-950 text-white selection:bg-indigo-500 selection:text-white overflow-hidden">
      
      {/* 1. ANIMATED NAVBAR */}
      <motion.nav 
        initial={{ y: -50, opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        transition={{ duration: 0.5 }}
        className="fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-8 py-5 bg-slate-950/80 backdrop-blur-md border-b border-slate-800"
      >
        <div className="text-xl font-bold tracking-wider text-indigo-400">ANIMATE.⚡</div>
        <div className="hidden md:flex space-x-8 text-sm font-medium text-slate-300">
          {['Home', 'Features', 'Projects', 'Contact'].map((item) => (
            <a key={item} href={`#${item.toLowerCase()}`} className="hover:text-indigo-400 transition-colors">
              {item}
            </a>
          ))}
        </div>
        <button className="bg-indigo-600 hover:bg-indigo-500 text-white text-sm px-4 py-2 rounded-full font-medium transition-all shadow-lg shadow-indigo-600/30">
          Get Started
        </button>
      </motion.nav>

      {/* 2. HERO SECTION */}
      <section id="home" className="relative flex flex-col items-center justify-center min-h-screen text-center px-4 pt-20">
        {/* Ambient Background Glow */}
        <div className="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-indigo-500/10 rounded-full blur-[120px] pointer-events-none" />

        <motion.div 
          variants={containerVariants}
          initial="hidden"
          animate="visible"
          className="max-w-4xl z-10"
        >
          <motion.span 
            variants={itemVariants}
            className="inline-block px-3 py-1 text-xs font-semibold uppercase tracking-widest text-indigo-400 bg-indigo-950/50 border border-indigo-800 rounded-full mb-6"
          >
            Next-Gen Web Experiences
          </motion.span>
          
          <motion.h1 
            variants={itemVariants}
            className="text-5xl md:text-7xl font-extrabold tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-white via-slate-200 to-slate-500 mb-6"
          >
            Crafting Digital Products <br />
            <span className="text-indigo-400">With Fluid Motion</span>
          </motion.h1>

          <motion.p 
            variants={itemVariants}
            className="text-lg md:text-xl text-slate-400 max-w-2xl mx-auto mb-10 leading-relaxed"
          >
            We build immersive, highly interactive interfaces that combine clean component architecture with bleeding-edge visual design patterns.
          </motion.p>

          <motion.div variants={itemVariants} className="flex flex-col sm:flex-row gap-4 justify-center">
            <motion.button 
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="flex items-center justify-center gap-2 bg-white text-slate-950 font-semibold px-6 py-3 rounded-xl transition-shadow hover:shadow-xl hover:shadow-white/10"
            >
              Explore Projects <FiArrowRight />
            </motion.button>
            <motion.button 
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="bg-slate-900 border border-slate-800 hover:bg-slate-850 px-6 py-3 rounded-xl font-semibold transition-colors"
            >
              Watch Demo
            </motion.button>
          </motion.div>
        </motion.div>
      </section>

      {/* 3. INTERACTIVE FEATURE GRID (SCROLL-TRIGGERED) */}
      <section id="features" className="py-24 px-8 max-w-7xl mx-auto border-t border-slate-900">
        <div className="mb-16 text-center md:text-left">
          <h2 className="text-3xl font-bold mb-4">Engineered For Performance</h2>
          <p className="text-slate-400 max-w-md">Smooth 60 FPS animations coupled with optimized client-side rendering frameworks.</p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          <FeatureCard 
            icon={<FiCpu className="text-2xl text-indigo-400" />}
            title="Hardware Accelerated"
            description="Leveraging CSS transforms and GPU rendering pathing for stutter-free user experiences."
          />
          <FeatureCard 
            icon={<FiLayers className="text-2xl text-purple-400" />}
            title="Clean Architecture"
            description="Modular component structures that make extending styles or animations friction-free."
          />
          <FeatureCard 
            icon={<FiActivity className="text-2xl text-pink-400" />}
            title="Fluid Interactivity"
            description="Dynamic hover triggers, localized mouse physics tracker adjustments, and gesture bindings."
          />
        </div>
      </section>
    </div>
  );
}

// 4. REUSABLE ANIMATED CARD COMPONENT
function FeatureCard({ icon, title, description }) {
  return (
    <motion.div 
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-100px" }}
      transition={{ duration: 0.5 }}
      whileHover={{ y: -8, borderColor: 'rgb(99, 102, 241)' }}
      className="p-8 rounded-2xl bg-slate-900/40 border border-slate-800/80 backdrop-blur-sm cursor-pointer transition-colors"
    >
      <div className="w-12 h-12 rounded-xl bg-slate-950 flex items-center justify-center mb-6 border border-slate-800">
        {icon}
      </div>
      <h3 className="text-xl font-bold mb-2 text-slate-100">{title}</h3>
      <p className="text-slate-400 text-sm leading-relaxed">{description}</p>
    </motion.div>
  );
}
